# ForgeGrid Configuration
# SQLite Database Authentication

# Application Settings
app.name=ForgeGrid
app.version=1.0.0
app.debug=false

# Shared database path (relative to project root or absolute). Ensure this file is committed.
db.path=forgegrid.db